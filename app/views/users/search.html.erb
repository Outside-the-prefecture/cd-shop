<div class="body">
    <% if @artist_or_genre_or_song_or_product=="1" %>
        <% if @artists.count==0 %>
            <h2>その歌手は登録されていません。</h2>
        <% else %>
            <% @artists.each do |artist| %>
                <h4 class="hit"><%= "#{artist.products.count}件の検索結果"%></h4>
                <h1>artist</h1>
                <h4><%= "#{artist.name}" %></h4>
                <% if artist.products.count==0 %>
                    <%= "#{artist.name}の商品は取り扱っておりません。" %>
                <% else %>
                    <div class="container">
                        <div class="row">
                            <div class="col-md-2">
                                <h5 class="genre">ジャンルから探す</h5>
                                <% @genres.each do |genre| %>
                                <div class="row">
                                    <div class="col-md-8 genre">
                                        <%= link_to genre.name, admin_genre_path(genre.id) %>
                                    </div>
                                </div>
                                <% end %>
                                <h5 class="genre search-artist">人気歌手から探す</h5>
                                <div class="row">
                                    <div class="col-md-8 genre">
                                        <% @all_ranks.each do |product| %>
                                        <% if @artistname==product.artist.name %>
                                        <% else %>
                                            <p class="artist-search-link">
                                               <%= link_to product.artist.name, admin_artist_path(product.artist.id) %>
                                            </p>
                                            <% @artistname = product.artist.name %>
                                        <% end %>
                                        <% end %>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-9 col-md-offset-1">
                                <div class="row">
                                    <% if admin_signed_in? %>
                                    <% artist.products.each do |product| %>
                                    <div class="col-md-2">
                                        <%= link_to admin_manager_product_path(product.id) do %>
                                        <%= attachment_image_tag product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                                        <div class="product-name-artist">
                                            <%= product.artist.name %>/&nbsp;&nbsp;<%= product.name.truncate(14) %><br/>
                                        </div>
                                        <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(product.price) %></strong></span><br/>
                                        <p class="linkborder"><%= product.status %></p>
                                        <% end %>
                                    </div>
                                    <% end %>
                                    <% else %>
                                    <% artist.products.each do |product| %>
                                    <div class="col-md-2">
                                        <%= link_to product_path(product.id) do %>
                                        <%= attachment_image_tag product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                                        <div class="product-name-artist">
                                            <%= product.artist.name %>/&nbsp;&nbsp;<%= product.name.truncate(14) %><br/>
                                        </div>
                                        <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(product.price) %></strong></span><br/>
                                        <p class="linkborder"><%= product.status %></p>
                                        <% end %>
                                    </div>
                                    <% end %>
                                    <% end %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% end %>
            <% end %>
        <% end %>

    <% elsif @artist_or_genre_or_song_or_product=="2" %>
        <% if @genres.count==0 %>
            <h2>そのジャンルはありません</h2>
        <% else %>
            <% @genres.each do |genre| %>
                <h4 class="hit"><%= "#{genre.products.count}件の検索結果"%></h4>
                <h1>genre</h1>
                <h4><%= "#{genre.name}" %></h4>
                <% if genre.products.count==0 %>
                    <%= "#{genre.name}の商品は取り扱っておりません。" %>
                <% else %>
                    <div class="container">
                        <div class="row">
                            <div class="col-md-2">
                                <h5 class="genre">ジャンルから探す</h5>
                                <% @genres.each do |genre| %>
                                <div class="row">
                                    <div class="col-md-8 genre">
                                        <%= link_to genre.name, admin_genre_path(genre.id) %>
                                    </div>
                                </div>
                                <% end %>
                                <h5 class="genre search-artist">人気歌手から探す</h5>
                                <div class="row">
                                    <div class="col-md-8 genre">
                                        <% @all_ranks.each do |product| %>
                                        <% if @artistname==product.artist.name %>
                                        <% else %>
                                            <p class="artist-search-link">
                                               <%= link_to product.artist.name, admin_artist_path(product.artist.id) %>
                                            </p>
                                            <% @artistname = product.artist.name %>
                                        <% end %>
                                        <% end %>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-9 col-md-offset-1">
                                <div class="row">
                                    <% if admin_signed_in? %>
                                    <% genre.products.each do |product| %>
                                    <div class="col-md-2">
                                        <%= link_to admin_manager_product_path(product.id) do %>
                                        <%= attachment_image_tag product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                                        <div class="product-name-artist">
                                            <%= product.artist.name %>/&nbsp;&nbsp;<%= product.name.truncate(14) %><br/>
                                        </div>
                                        <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(product.price) %></strong></span><br/>
                                        <p class="linkborder"><%= product.status %></p>
                                        <% end %>
                                    </div>
                                    <% end %>
                                    <% else %>
                                    <% genre.products.each do |product| %>
                                    <div class="col-md-2">
                                        <%= link_to product_path(product.id) do %>
                                        <%= attachment_image_tag product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                                        <div class="product-name-artist">
                                            <%= product.artist.name %>/&nbsp;&nbsp;<%= product.name.truncate(14) %><br/>
                                        </div>
                                        <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(product.price) %></strong></span><br/>
                                        <p class="linkborder"><%= product.status %></p>
                                        <% end %>
                                    </div>
                                    <% end %>
                                    <% end %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% end %>
            <% end %>
        <% end %>

    <% elsif @artist_or_genre_or_song_or_product=="3" %>
        <h4 class="hit"><%= "#{@songs.count}件の検索結果"%></h4>
        <h2>song</h2>
        <div class="container">
            <div class="row">
                <div class="col-md-2">
                    <h5 class="genre">ジャンルから探す</h5>
                    <% @genres.each do |genre| %>
                    <div class="row">
                        <div class="col-md-8 genre">
                            <%= link_to genre.name, admin_genre_path(genre.id) %>
                        </div>
                    </div>
                    <% end %>
                    <h5 class="genre search-artist">人気歌手から探す</h5>
                    <div class="row">
                        <div class="col-md-8 genre">
                            <% @all_ranks.each do |product| %>
                            <% if @artistname==product.artist.name %>
                            <% else %>
                                <p class="artist-search-link">
                                    <%= link_to product.artist.name, admin_artist_path(product.artist.id) %>
                                </p>
                                <% @artistname = product.artist.name %>
                            <% end %>
                            <% end %>
                        </div>
                    </div>
                </div>
                <div class="col-md-9 col-md-offset-1">
                    <div class="row">
                        <% if admin_signed_in? %>
                        <% @songs.each do |song| %>
                        <div class="col-md-2">
                            <%= link_to admin_manager_product_path(song.disc.product.id) do %>
                            <%= attachment_image_tag song.disc.product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                            <div class="product-name-artist">
                                <%= song.disc.product.artist.name %>/&nbsp;&nbsp;<%= song.disc.product.name.truncate(14) %><br/>
                            </div>
                            <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(song.disc.product.price) %></strong></span><br/>
                            <p class="linkborder"><%= song.disc.product.status %></p>
                            <% end %>
                        </div>
                        <% end %>
                        <% else %>
                        <% @songs.each do |song| %>
                        <div class="col-md-2">
                            <%= link_to product_path(song.disc.product.id) do %>
                            <%= attachment_image_tag song.disc.product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                            <div class="product-name-artist">
                                <%= song.disc.product.artist.name %>/&nbsp;&nbsp;<%= song.disc.product.name.truncate(14) %><br/>
                            </div>
                            <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(song.disc.product.price) %></strong></span><br/>
                            <p class="linkborder"><%= song.disc.product.status %></p>
                            <% end %>
                        </div>
                        <% end %>
                        <% end %>
                    </div>
                </div>
            </div>
        </div>

    <% else %>
        <h4 class="hit"><%= "#{@products.count}件の検索結果"%></h4>
        <h2>product</h2>
                <div class="container">
                    <div class="row">
                            <div class="col-md-2">
                                <h5 class="genre">ジャンルから探す</h5>
                                <% @genres.each do |genre| %>
                                <div class="row">
                                    <div class="col-md-8 genre">
                                        <%= link_to genre.name, admin_genre_path(genre.id) %>
                                　　 </div>
                                </div>
                                <% end %>
                                <h5 class="genre search-artist">人気歌手から探す</h5>
                                <div class="row">
                                    <div class="col-md-8 genre">
                                        <% @all_ranks.each do |product| %>
                                        <% if @artistname==product.artist.name %>
                                        <% else %>
                                          <p class="artist-search-link">
                                            <%= link_to product.artist.name, admin_artist_path(product.artist.id) %>
                                          </p>
                                          <% @artistname = product.artist.name %>
                                        <% end %>
                                        <% end %>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-9 col-md-offset-1">
                                <div class="row">
                                    <% if admin_signed_in? %>
                                    <% @products.each do |product| %>
                                    <div class="col-md-2">
                                        <%= link_to admin_manager_product_path(product.id) do %>
                                        <%= attachment_image_tag product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                                        <div class="product-name-artist">
                                            <%= product.artist.name %>/&nbsp;&nbsp;<%= product.name.truncate(14) %><br/>
                                        </div>
                                        <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(product.price) %></strong></span><br/>
                                        <p class="linkborder"><%= product.status %></p>
                                        <% end %>
                                    </div>
                                    <% end %>
                                    <% else %>
                                    <% @products.each do |product| %>
                                    <div class="col-md-2">
                                        <%= link_to product_path(product.id) do %>
                                        <%= attachment_image_tag product, :jacket_image, :fill, 110,110, fallback: "",size: "100*100" %><br/>
                                        <div class="product-name-artist">
                                            <%= product.artist.name %>/&nbsp;&nbsp;<%= product.name.truncate(14) %><br/>
                                        </div>
                                        <p class="kakaku">価格</p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;<span class="price-color"><strong>￥<%= converting_to_jpy(product.price) %></strong></span><br/>
                                        <p class="linkborder"><%= product.status %></p>
                                        <% end %>
                                    </div>
                                    <% end %>
                                    <% end %>
                                </div>
                            </div>
                    </div>
                </div>
    <% end %>
</div>