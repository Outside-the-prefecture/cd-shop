<div class="nested-fields">
    <%= f.text_field :name %>
    <%= link_to_remove_association "ディスクを削除する", f %>
</div>

<div class="discs">
	<h4>曲</h4>
  	<%= f.fields_for :songs do |song| %>
  	<%= render "song_fields", f: song %>
  	<% end %>
  	<div class="links">
  	<%= link_to_add_association "曲を追加", f, :songs %>
  	</div>
 </div>
